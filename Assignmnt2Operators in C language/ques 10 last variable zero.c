#include<stdio.h>
int main()
{
    int n,a,b;
    printf("\nenter the number =");
    scanf("%d",&n);
    a=n/10;
    b=a*10;
    printf("the last digit of a number stored in a variable as zero %d",b);
    return 0;
}