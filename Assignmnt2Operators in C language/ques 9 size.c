#include<stdio.h>
int main()
{
    int a;
    char b;
    double c;
    float d;
    printf("\nthe size of int is = %d",sizeof(a));
    printf("\nthe size of char is = %d",sizeof(b));
    printf("\nthe size of double is = %d",sizeof(c));
    printf("\nthe size of float is = %d",sizeof(d));
}